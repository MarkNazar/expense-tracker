import styled from "styled-components";

const IconWithText = styled.button`
  display: flex;
  align-items: center;
  gap: 8px;
  background: none;
  border: none;
  font-size: 1rem;
`;
export default IconWithText;
